// Model id on the configured OpenAI-compatible gateway. Override with AI_MODEL
// when the gateway's model lineup changes — no code edit needed.
export const AI_MODEL = process.env.AI_MODEL || "gpt-5.6-terra";
